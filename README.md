# Installation

## requirements
```sh
pip3 install -r requirements.txt
```

# Note

## Running
Jupyter should be run from the `ipynb/` directory.

The main python script that is `run.py`, should be run from the `python/` directory.
